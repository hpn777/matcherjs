{
  "name": "matcherjs",
  "version": "1.0.0",
  "description": "Order matching engine (TypeScript)",
  "license": "MIT",
  "main": "dist/matcher.js",
  "types": "dist/matcher.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc --watch",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
